输入：FACTS（JSON）如下（由后端生成并注入）：
FACTS = {{health_facts_json}}

请生成 module="health" 的报告 JSON，要求：

必须解释：综合健康分（score_health）与五维（vitality/responsiveness/resilience/governance/security）的现状、短板与影响。

必须对齐 CHAOSS 思路：

Activity（活跃度）优先引用 activity/openrank/new_contributors/growth 等指标（若 facts 有）。

Responsiveness（响应性）优先引用 time to first response / close / backlog age（若 facts 有）。

Risk（风险/可持续性）优先引用 bus factor、inactive contributors、retention（若 facts 有）。

Governance/Security 优先引用 scorecard、governance_files、github_health_percentage（若 facts 有）。

sections 建议包含（按顺序）：

Executive Summary（3条要点）

五维解读（逐维：状态→证据→解释→影响）

根因线索（只基于 facts；如 backlog/响应时间/增长放缓）

行动建议（P0/P1/P2，明确 steps 与 metrics_to_watch）

数据完整性（缺失/时间窗不足/异常值说明）

只要出现数值（例如“首响 8h、总分 85、scorecard 7.4”），必须在 evidence 列表里标注对应 key/value/dt。

如果没有对标数据，就不要输出“分位数/超过同类多少”这类表述；只能说“当前候选集合内缺少对标数据”。

输出必须是严格合法 JSON。